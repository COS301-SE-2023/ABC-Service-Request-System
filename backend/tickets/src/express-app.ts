// import express from 'express';
// import cors from 'cors';
// import router from './api/ticket.controller';

// const expressApp = async (app) => {
//     app.use(express.json());
//     app.use(cors());
//     app.use(express.static(__dirname + '/public'));

// }